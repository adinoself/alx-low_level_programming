#include <stdio.h>

/**
 * main - this prints a statement using printf
 *
 * Return: should return 0 as required
 */

int main (void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return(0);
}
